Copy of the original portfolio folder for experimentation.

Notes:
- This copy references the original `style.css` and image assets in `../portfolio/` to avoid duplicating binaries.
- To make a fully independent copy, copy the image files from `portfolio/` into this folder and update the asset paths in the HTML/CSS accordingly.